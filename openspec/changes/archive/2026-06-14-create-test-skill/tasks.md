## 1. Create the skill directory

- [x] 1.1 Create `.opencode/skills/test/` directory if it doesn't exist

## 2. Write the skill file

- [x] 2.1 Create `.opencode/skills/test/SKILL.md` with YAML frontmatter (`name: test`, `description` with trigger keywords) and Markdown body containing all test, lint, and quality check commands

## 3. Verification

- [x] 3.1 Verify the skill file exists at `.opencode/skills/test/SKILL.md`
- [x] 3.2 Verify YAML frontmatter parses correctly (valid name, description fields)
- [x] 3.3 Verify all documented commands match AGENTS.md and `package.json` scripts
